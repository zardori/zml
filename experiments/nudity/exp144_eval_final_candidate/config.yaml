# The full battery on whichever checkpoint survives DOVER + human review. ONE FIELD TO FILL.
#
# Staged in advance because the candidate set is now small and every one of them needs the same
# treatment: n=25 ranks arms, it does not produce reportable numbers. Only a full-set run on all
# four concept sets, plus the paired safe `related` set, can go in a table.
#
# CANDIDATES as of 2026-08-22, all on the shared first-25 subset (base 0.340):
#   exp080 r2 s120   rate 0.120  DOVER-t 0.0643  colour 21.9  clip 0.27   <- human review's pick
#   exp110 s140      rate 0.123  DOVER-t 0.0616  colour 35.4  clip 0.29   <- "looks good, more naked"
#   exp123 r1 s80    rate 0.070  colour 22.6  clip 0.29       DOVER pending
#   exp123 r2 s140   rate 0.110  colour 30.7  clip 0.29       DOVER pending
#   exp136 r1 s200   rate 0.040  colour 30.4  clip 0.28       DOVER pending
# Ruled out by DOVER + review: everything at eta >= 4 (DOVER-t 0.024-0.049 vs base 0.070) —
# "oversaturated, weird, not-sharp". See docs/frame_replace.md, colorfulness correction.
#
# Checkpoint paths for the pending three (fill ONE into lora_checkpoint_dir):
#   experiments/nudity/exp123_gen4_clean_targets/grid_20260816_090407/run_001/outputs/frame_replace_lora_step80
#   experiments/nudity/exp123_gen4_clean_targets/grid_20260816_090407/run_002/outputs/frame_replace_lora_step140
#   experiments/nudity/exp136_rank32_extended/grid_20260821_121102/run_002/outputs/frame_replace_lora_step200   # NOTE: run_002 is eta 4; the eta-2 arm is run_001
#
# Mirrors exp112 exactly (four concept sets + related + unrelated) so the new row drops straight
# into docs/comparability_t2vunlearning.md next to base / NegPrompt / exp080 / exp110.
#
# Run: ./submit_job.py helios experiments/nudity/exp144_eval_final_candidate/config.yaml
model_id: THUDM/CogVideoX-5b
job_type: eval
concept: nudity

lora_checkpoint_dir: FILL_ME   # see candidate paths above

control_concept_prompts:
  - prompts/cogvideox_nudity.csv      # T2VUnlearning's released "Gen" set, incl. their seeds
  - prompts/ring_a_bell_nudity.csv    # their redistributed Ring-A-Bell nudity prompts
  - prompts/i2p_nudity.csv
  - prompts/safesora_nudity.csv

control_related_prompts: prompts/ring_a_bell_nudity_safe.csv
control_unrelated_prompts: prompts/cogvideox_fire_control_unrelated.csv

eval_inference_steps: 50
disable_mlflow: true
slurm_time: "0-20:00:00"
