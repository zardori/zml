method: frame_replace
model_id: THUDM/CogVideoX-5b
concept: nudity   # eval scores with NudeNet (zml/unlearn/eval.py concept selector)

# Same dataset/regime as exp062 run 3 (human-reviewed 21-triple dataset). Only steps/save_interval
# change: zoom into the 0-100 step window with a checkpoint+eval every 20 steps, instead of exp062's
# single step-100 checkpoint. exp062 found the step-100 checkpoint was mostly generation collapse
# (8/10 concept videos near-blank, motion_score_mean ~0.03) rather than genuine clothed erasure, with
# real content (and nudity) reappearing once generation stabilized by step 600. This run finds out
# whether there's a stable, non-collapsed low-nudity point somewhere in 0-100, or whether collapse and
# erasure are inseparable at this LR/regime.
metadata_file: experiments/exp061_split_nudity_dataset/metadata_human_filtered.json
latents_dir: experiments/exp061_split_nudity_dataset/outputs_20260802_223148/latents
retention_metadata_file: experiments/exp041_preservation_precompute/outputs_20260623_204602/metadata.json
retention_latents_dir: experiments/exp041_preservation_precompute/outputs_20260623_204602/latents
retention_weight: 1.0

nonfire_frame_weight: 0.0
erase_input_latent: original
erase_loss_space: velocity
erase_esd_eta: 2

control_concept_prompts: prompts/cogvideox_nudity.csv
control_related_prompts: prompts/cogvideox_fire_control_unrelated.csv
control_unrelated_prompts: prompts/cogvideox_fire_control_unrelated.csv

lora_rank: 8
lora_alpha: 8.0
lora_dropout: 0.0

gradient_accumulation_steps: 4
lr_scheduler: constant
learning_rate: 0.0005
steps: 100
save_interval: 20

timestep_min: 400
timestep_max: 1000

eval_num_prompts: 10
eval_inference_steps: 50
global_seed: 42

slurm_time: "0-08:00:00"
