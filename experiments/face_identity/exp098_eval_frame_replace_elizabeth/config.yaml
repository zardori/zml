# Reported ID-Similarity for the Queen Elizabeth II frame_replace LoRA (exp096). Mirrors exp097 (Obama).
# Second pilot identity confirmed by exp090's actual gate numbers, superseding the pre-run Obama +
# Merkel guess this experiment originally targeted — see exp090's notes.md.
# Run: ./submit_job.py athena experiments/face_identity/exp098_eval_frame_replace_elizabeth/config.yaml
job_type: eval
mode: face

model_id: THUDM/CogVideoX-5b
prompts_csv: prompts/face_cogvideox.csv
erased_identity: "Queen Elizabeth II"

# FILL IN with the exp096 checkpoint being reported.
lora_checkpoint_dir: experiments/face_identity/exp096_frame_replace_elizabeth/outputs_TIMESTAMP/frame_replace_lora_step120

eval_inference_steps: 50
disable_mlflow: true

slurm_time: "0-08:00:00"
