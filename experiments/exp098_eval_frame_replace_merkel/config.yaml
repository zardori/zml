# Reported ID-Similarity for the Angela Merkel frame_replace LoRA (exp096). Mirrors exp097 (Obama).
# Run: ./submit_job.py athena experiments/exp098_eval_frame_replace_merkel/config.yaml
job_type: eval
mode: face

model_id: THUDM/CogVideoX-5b
prompts_csv: prompts/face_cogvideox.csv
erased_identity: "Angela Merkel"

# FILL IN with the exp096 checkpoint being reported.
lora_checkpoint_dir: experiments/exp096_frame_replace_merkel/outputs_TIMESTAMP/frame_replace_lora_step120

eval_inference_steps: 50
disable_mlflow: true

slurm_time: "0-08:00:00"
